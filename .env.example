# Simple Memory MCP 环境配置示例
# 复制此文件为 .env 并根据需要修改配置

# Web服务器端口（默认8011）
PORT=8011

# 数据库文件路径（默认data/memories.db）
DB_PATH=./data/memories.db

# 日志级别（可选值: error, warn, info, debug）
LOG_LEVEL=info

# 日志文件路径（可选）
LOG_FILE=./logs/app.log

# 环境类型（development, production, test）
NODE_ENV=production

# 是否启用详细日志（true/false）
VERBOSE_LOGGING=false

# 数据库连接超时时间（毫秒）
DB_TIMEOUT=5000

# Web服务器主机地址（默认localhost）
HOST=localhost

# 是否启用CORS（true/false）
ENABLE_CORS=true

# 最大记忆内容长度（字符数）
MAX_CONTENT_LENGTH=100000

# 最大记忆标题长度（字符数）
MAX_TITLE_LENGTH=200

# 是否启用搜索功能（true/false）
ENABLE_SEARCH=true

# 备份目录路径（可选）
BACKUP_DIR=./backups

# 自动备份间隔（小时，0表示禁用）
AUTO_BACKUP_INTERVAL=24
